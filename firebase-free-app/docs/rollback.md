# Rollback Guide

- Firebase Hosting keeps previous versions.
- Open Hosting > spx-netdev > Releases.
- Select a previous release and roll back.
- If Firestore data was changed, restore from the backups branch using the Firestore Restore workflow.
